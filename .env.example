# De publieke Vite-frontend bevat bewust geen provider- of databasegeheimen.
# Lokale Worker-secrets horen in `.dev.vars` (zie `.dev.vars.example`).

# Optioneel voor een GitHub Pages-build van alleen de statische interface.
# De volledige accountapp moet samen met de Worker op dezelfde origin draaien.
VITE_BASE=/
